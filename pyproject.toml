[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: no external dependencies, runs on any dev machine",
    "stand: needs a provisioned stand VM; skipped when unreachable",
]
# Default run = fast unit tests only. Stand tests are opt-in: `pytest -m stand`.
addopts = "-m unit -ra"
