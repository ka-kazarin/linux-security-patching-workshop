# --- Development and agent working files (not for students, kept out of git) ---
/AGENTS.md
/CLAUDE.md
/PRACTICES-PROMPT.md
/rules/
/docs/process/
/docs/stand-spec.md
/.claude/

# --- Working artifacts ---
.vagrant/
*.retry
*.log

# Scan and report output (generated, not committed; fallback is the exception below)
/scan/reports/
/tests/reports/
*.junit.xml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# OS
.DS_Store

# results/ holds generated artifacts (scan/delta/bench/verify/attack output) --
# regenerated by the make targets, refreshed before the event, never committed.
# Only the README (which explains the directory) is tracked.
/results/*
!/results/README.md
manual-test*
tests-feedback*
presentation-plan-initial_from-claude.md