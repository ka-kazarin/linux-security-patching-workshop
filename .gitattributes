# Append-only logs: union merge so parallel branches never conflict.
# (docs/process/ is outside git, but the rule stays in place in case the
#  process files ever get locally versioned in a separate branch/fork.)
docs/process/context.md merge=union
