# Append-only логи: union-merge, чтобы параллельные ветки не конфликтовали.
# (docs/process/ вне git, но правило остаётся на случай локального версионирования
#  процессных файлов в отдельной ветке/форке.)
docs/process/context.md merge=union
